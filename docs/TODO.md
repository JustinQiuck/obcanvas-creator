# 执行待办

## 生成段融合（当前规划优先级）

当前源码 0.8.0。已完成的项目删除和多模型分别见 [删除验收](validation/project-deletion.md)、[多模型验收](validation/model-profiles.md)。以下按 [生成段融合计划](generation-segments-plan.md) 推进，不引入参考片研究。

- [x] GS00：明确镜头、生成段、生成尝试及按镜头采用的关系，更新后续路线。
- [x] GS00 复审：对照 0.8.0 代码修正全片顺序、补拍覆盖、时间窗、尝试快照、输入模式、旧采用和路线冲突，见 [审查记录](validation/generation-segments-review.md)。仅规划修正，未实现功能。
- [ ] GS01 / DS02：保留真实分镜质量门槛，接通正式入卡、阶段 Skill 选择/自定义与作品方向、项目全片顺序、生成段记录和手动编排；主段/补拍/历史方案分开。
- [ ] GS02 / DS03–DS04：参考输入与关键帧、段内时间窗、逐段提示词、制作包及交付前的首次尝试快照；外部实际输入单独补录；可选 AI 分段建议。
- [ ] GS03 / DS04、DS06：尝试的视频导回、采用区间、单镜补拍和历史保留；兼容直接导入及旧采用未标区间。
- [ ] GS04 / DS06、R4：真实截帧、跨段续接及受影响输入复核。
- [ ] 按各切片分别验证保存、旧数据兼容、删除恢复、隔离宿主及真实小样。

## 0.7.0 剧本项目库（历史验收）

范围见 [项目库调整](project-library-plan.md)。用户纠正界面与归属后，优先完成此切片；后面的 0.6.0／DS 记录保留历史状态。

- [x] 项目记录与持久化归属，旧内容只读归入“原有剧本项目”，资产独立于场次创建。
- [x] 拆出剧本、资产库、分镜、生成素材和画布工作区，清理剧本卡内的 AI 长表单。
- [x] 同项目跨场次复用；列表、引用、模型上下文和默认 Skill 按项目管理。
- [x] 类型、构建、59 项逻辑测试、9 项隔离宿主操作通过，见 [验收](validation/project-library.md)。
- [x] 完整重启 2 项检查通过，项目／工作区／编辑内容与原项目文件恢复正确。
- [x] 备份并更新当前资料库至 0.7.0，正式重启后确认 2 份剧本、7 项资产在原有项目，16 份项目文件校验值不变。
- [ ] 用户实际操作验收。

## 0.6.0 历史进展

以下记录 0.6.0 时点进展，当前源码版本见本页开头。资产 Skill 选择与自定义继续可用；DS01 的规则包、分镜预览、任务保存和隔离 Obsidian 验收已经完成，真实模型质量对照尚未完成。DS01–DS05 细化 R3，DS06 对应 R4；不重复实施已完成的 A01–A05。D07/D08 保留为历史切片；P1（D06）只表示技术验证完成。历史结果见 [P0-A](validation/p0-a.md)、[P0-B](validation/p0-b.md)、[P0-C](validation/p0-c.md) 和 [P1](validation/p1.md)。产品范围见 [改造计划](plan.md)，切片与文件安排见 [开发计划](development-plan.md)。只有实际完成并验证的条目才勾选；构建通过不能代替 Obsidian 内的操作验收。

## Drama Skills 接入

范围、字段映射、切片验收与停止条件见 [接入计划](drama-skills-integration-plan.md)。工程与创作质量分别记录。

- [x] DS00：固定上游 `4c40ca6101648579b2455d0c0d890ce701f05173`，完成价值、许可、架构差异与实施计划；更新本地路线入口。
- [x] DS01 工程实现：审定并打包分镜规则，完成可编辑短范围预览、原文校验、独立任务存储、取消、冲突保护和规则快照；不创建正式镜头卡。
- [x] DS01 工程验收：53 项逻辑测试、4 项隔离宿主操作、1 项完整重启恢复通过；现有资产整理 10 项与 Skill 选择 5 项宿主回归通过。证据见 [DS01 验收记录](validation/storyboard-ds01.md)。
- [x] DS01 正式安装：备份旧插件并更新当前 Obsidian 资料库至 0.6.0；重启后确认剧本卡出现“分镜与关键帧 · 试点”和生成入口，原项目文件校验值不变。
- [ ] DS01 真实质量：使用相同输入和文字模型比较最低基线与适配规则，分别记录结构、镜头修改量和创作评分；达到门槛后才进入 DS02。
- DS02：执行与完成状态合并到上方 GS01，保留分阶段选择/自定义、作品方向及质量门槛，不重复建待办。
- DS03：执行与完成状态合并到上方 GS02，保留资产图片提示词、镜头关键帧、段参考槽位与外部挂图计划。
- DS04：执行与完成状态合并到上方 GS02–GS03，核对 H3 手动输入；段制作包、尝试导回和逐镜选片。
- [ ] DS05：增加可选 Drama 资产方法及有来源/有效范围的持续状态，验证身份复用、变体选择和已有绑定保护。
- DS06 / R4：执行与完成状态合并到上方 GS03–GS04，覆盖实际区间、A/B 交叉剪辑与独立机位续接。
- [ ] 按所到切片分别完成必要逻辑检查、隔离宿主验收、真实文字小样和用户制作试用；未实际执行的验证保持未完成。

## AI 助手与资产准备（0.4.0）

范围和逐项验收见 [剧本到拍摄资产计划](agent-assets-plan.md)。以下 A 项勾选代表实现与模拟接口验证通过，真实服务及用户验收分别记录。

- [x] 记录用户确认的结构：一套文字模型配置、任务对应的内置 Skill、插件提供项目读写工具；独立实现，不复制 huobao-drama 正文或后端。
- [x] 编写 A01–A05 范围、数据保留规则、操作流程与验收标准，并同步现有路线。
- [x] A01：增加道具、剧本资产引用、参考用途及任务草稿；验证跨场次复用和旧记录兼容。
- [x] A02：核实首个文字接口，实现中文设置、主动连接测试及凭据处理；验证设置不会被草稿保存覆盖。
- [x] A03：打包内置资产提取 Skill，实现任务执行、依据与格式校验、取消和错误反馈。
- [x] A04：实现可编辑清单、已有资产复用、确认应用和草稿恢复；验证过期结果保护及部分失败重试。
- [x] A05：实现资产参考图绑定、用途确认与缺失项定位，完成隔离 Obsidian 全流程和重开验证。
- [x] 按既有更新授权备份并更新当前资料库至 0.4.0，原生设置页可用，6 份原项目笔记校验值不变。
- [ ] 真实文字接口验证：使用确定的服务、模型和中性内容，单独记录实际调用结果。
- [ ] 用户操作验收：能从剧本整理资产、绑定图片并看清缺失项；与代码和宿主测试分别记录。

## 自由画布总体路线

- [x] 明确用户确认的主界面、制作流程和数据保留边界，修正开发顺序。
- [x] R0：制作独立可操作原型，支持自由卡片、连线、按需详情与待办定位。
- [x] R0：完成浏览器操作和视觉检查，记录 [预览限制](validation/r0.md)；真实文件导入未验证。
- [x] R0：用户确认原型方向「差不多这个意思」，授权更新插件实操。
- [x] R1：自由节点和连线接入 Vault，保留 0.2.0 记录和草稿；[实机及重启验收](validation/r1.md) 通过。
- [ ] R2：已随 R1 接入基础用途/候选关系、可定位待办和切换前保存；完整制作状态与续接复核尚待完善。
- [ ] R3：基于已完成的 A01–A05，按 DS01–DS05 逐步接入分镜、图片/视频提示词与制作包，先验证分镜价值。
- [ ] R4：按 DS06 实现视频选帧、采用区间和续接依赖。

## 仓库初始化

- [x] 克隆独立仓库。
- [x] 整理项目说明、已确认路线和分阶段验收标准。
- [x] 明确全中文操作与 RunningHub 外部生成的边界。
- [x] 将仓库及原有文档迁入当前目录下的 `obcanvas-creator`，明确源码复用的目标目录。
- [x] 写入 D01–D08 开发顺序、首批验收标准及项目工作约定。

## P0：最小接入与可靠保存

- [x] 核对源码和官方 API，形成 [接入设计](obsidian-integration.md)；运行验证结果见 P0-A／B／C。

- [x] D01：建立插件骨架、隔离测试资料库和中文视图，记录 Obsidian 运行时版本并验证加载与卸载。
- [x] D02：固定最小记录格式和稳定 ID，实现读取与受控更新，保留原有正文和未知属性。
- [x] D03：实现镜头卡与笔记双向读写，验证并发冲突、保存失败和关闭重开；记录 P0-A 验收。
- [x] D04：按计划将所需原画布文件复制到本仓库，记录实际来源和许可证，完成事件及主题适配。
- [x] D04：保存布局与视口，检查相邻笔记输入和视图关闭后的资源释放；记录 P0-B 验收。
- [x] D05：实现库内参考图与视频关联、预览和可靠定位，验证改名与缺失处理；记录 P0-C 验收。
- [x] 完成 P0 连贯验收，D06 已通过 P1 验收，不以构建通过代替宿主操作通过。

## P1：镜头板与视频归档

对应开发切片 D06，前置条件为 P0 通过。

- [x] 以当前资料库作为项目边界，建立场次、镜头卡片与明确的镜头顺序，展示镜头意图、景别机位和参考图。
- [x] 将已有视频／图片导入指定镜头，登记可靠文件位置与可获得的技术属性。
- [x] 支持多候选、同一素材供多镜头使用、采用与退回决定。
- [x] 根据计划和选择记录显示待素材、待选片、待重做等状态。
- [x] 验证 README 中三个镜头的完整循环及重开恢复。
- [x] 检查空白页、导入失败、素材离线和保存反馈均可通过中文界面理解与处理。

## P2：选区间与关键帧

对应历史切片 D07，现归 R4；排在 A01–A05 与 R3 后续之后。

- [ ] 保存采用入出点，从视频选帧并记录真实来源时间及用途。
- [ ] 区分连续续接与独立机位参考，核对续接帧和采用出点。
- [ ] 源素材或区间变化时，仅将实际依赖的镜头标记为需复核。
- [ ] 验证来源可追溯、原始素材未被修改、独立镜头不受影响。

## P3：Skill 与制作包

对应历史切片 D08，现归 R3 后续；前置为 A01–A05 完整资产流程，不再等待 P2／R4 选帧完成。

- [ ] 映射现有分镜 Skill 输出，包含拍摄理由、静态关键帧提示词和视频动作提示词。
- [ ] 导入前显示差异，重复导入不重复建镜头，更新内容时保留采用记录。
- 段制作包与按镜头归档的执行状态见 GS02–GS03；单镜是只含一镜的生成段，不单独重复实施。
- [ ] 验证未知事实保留 TBD，Agent 整理与建议不自动替用户采用素材。

## 待实施时确认

- Obsidian 运行时版本：1.13.7（本机实测）；隔离测试资料库已建立在 `.local/test-vault/`。
- 正式素材目录与默认导入方式（复制入库或关联外部路径）：TBD。
- 首轮中性测试视频：FFmpeg 合成的 640 × 360、2 秒 H.264 MP4；PNG 测试图同尺寸。生成脚本为 `scripts/prepare-media-fixtures.mjs`，二进制只在 `.local/`。

这些信息不阻塞文档初始化；实际安装与正式素材导入前需要确认。首轮不迁移历史项目，不发起远程生成。

## 0.4.1：资产整理无结果反馈

- [x] 确认正式界面失败阶段，修复单项失败丢弃整份清单及等待无文字状态的问题。
- [x] 保存逐项问题、保留有效资产，兼容纯格式差异；无有效条目不能确认。
- [x] 39 项逻辑测试、隔离宿主 10 项操作和 2 项重启检查通过，备份并更新当前资料库。
- [ ] 使用真实模型重新整理后的效果验证；旧版失败响应未保留，无法追溯具体字段。

## 0.4.2：卡片删除

- [x] 卡片详情提供删除按钮，卡片聚焦时 Delete／Backspace 打开确认；不拦截文本退格。
- [x] 卡片笔记移入资料库回收站，保留原媒体、其他记录、入向引用、镜头顺序和布局，便于恢复。
- [x] 纯素材卡仅移除当前场次的关联；已采用视频需先取消采用，部分保存失败可重试。
- [x] 42 项逻辑测试、5 项隔离宿主操作及 1 项完整重启检查通过；更新当前资料库至 0.4.2，原项目文件保持不变。

## 0.5.0：资产 Skill 选择与自定义

- [x] 资产提取与未来分镜设计分阶段，提供通用剧情／音乐 MV 两项内置规则。
- [x] 剧本 Skill 选择、项目默认、内置复制编辑、Markdown 导入／导出及中文错误处理。
- [x] 资料库保存独立 Skill 配置，历史任务保留版本、原规则和实际请求协议快照；不改写旧资产。
- [x] 47 项逻辑测试、原资产流程 10 项实机回归、Skill 5 项实机检查及 1 项重启恢复检查通过；更新正式资料库至 0.5.0。
- [ ] 真实模型下自定义 Skill 的提取质量与用户使用验收；MV 节奏／分镜仍属后续独立阶段。
