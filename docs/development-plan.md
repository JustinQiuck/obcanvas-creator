# 开发计划

当前状态：D01–D05 已实现并通过 [P0-A](validation/p0-a.md)、[P0-B](validation/p0-b.md) 与 [P0-C](validation/p0-c.md) 验收；D06–D08 尚未实现。任务完成状态统一维护在 [执行待办](TODO.md)，本文件定义实施顺序、文件范围和验收证据。

## 固定工作目录

- 本地开发根目录：`/Users/aliceboy/项目文件/闲来无事/obcanvas-creator`。
- 远端仓库：[JustinQiuck/obcanvas-creator](https://github.com/JustinQiuck/obcanvas-creator)。
- 复用来源：`/Users/aliceboy/项目文件/画布`。选中文件复制到本仓库后再适配，以本仓库版本为准，原项目继续保留。
- 先前位于 `/Users/aliceboy/项目文件/obcanvas-creator` 的仓库已整体迁入当前目录，四份原有文档及 Git 关联保留。
- 本仓库应能独立安装依赖和构建，不使用指向原画布的绝对 import、软链接或外部 `file:` 包依赖。

## 开发顺序

| 切片 | 目标 | 前置条件 | 交付与通过条件 |
| --- | --- | --- | --- |
| D01 | 插件骨架与隔离测试环境 | 当前计划 | 能在测试库加载“影视画布”视图，关闭后资源释放；记录实际 Obsidian 运行时版本 |
| D02 | 固定最小记录格式 | D01 | 一张场次笔记、一张镜头笔记可稳定读取，重命名不改变记录身份，未知属性和正文得到保留 |
| D03 | 镜头卡与笔记双向读写 | D02 | 两个入口编辑同一份内容，保存失败和并发冲突可见，关闭重开恢复；完成 P0-A |
| D04 | 接入原画布交互与布局 | D03 | 平移、缩放、移动卡片及重开恢复；不影响旁边笔记的输入和主题；完成 P0-B |
| D05 | 库内图片和视频接入 | D04 | 一张图和一段短视频能关联、显示／播放及重开定位，缺失与解析失败反馈准确；完成 P0-C |
| D06 | 一场戏的候选与采用管理 | P0 验收通过 | 三个镜头呈现已采用、待素材、待重做；支持多候选和一素材多镜头；完成 P1 |
| D07 | 采用区间和关键帧关联 | P1 验收通过 | 来源可追溯，变更只提示实际依赖的镜头；完成 P2 |
| D08 | 分镜 Skill 导入和单镜制作包 | P2 验收通过 | 不重复创建镜头、不覆盖采用结果；输出供外部生成使用的参考文件与提示词；完成 P3 |

D01–D03 是第一批开发范围。P0-A 验收后进入 D04，不提前加入完整候选管理、截图、Agent 面板或生成服务。各阶段的产品规则继续遵循 [总计划](plan.md)，此处不新增产品功能。

## D01：插件骨架与测试环境

预期新增文件：`package.json`、包管理锁文件、`tsconfig.json`、构建配置、`manifest.json`、`src/main.ts`、`src/obsidian-view.tsx`、`src/ui/workbench.tsx`、视图范围内的样式文件。这些工程文件已建立，具体文件以本仓库为准。

实施内容：

1. 采用 TypeScript、React 和 Obsidian 插件入口；开发时核实兼容版本并锁定依赖。
2. 用自定义视图挂载 React，提供“打开影视画布”的中文入口，处理关闭、卸载和重新打开。
3. 在本地 `.local/test-vault/` 建立隔离测试资料库，仅放中性测试内容。该目录不进入 Git；插件源码和可复现测试夹具进入 Git。
4. 构建产物安装到隔离测试库，记录实际运行时版本；不把安装包版本直接当作运行时验收证据。

验收：构建成功；测试库内可以打开中文视图；重复打开和关闭没有多余 React 根节点或残留监听。仅显示空界面不代表 P0-A 完成。

## D02：记录约定与读取

预期新增：`src/model.ts`、`src/storage/vault-records.ts`；视实际需要添加记录读写的测试夹具。

- 固定场次 ID、镜头 ID、文件类型与格式版本。P0 的镜头只需要标题、所属场次、正文和参考素材关联；候选采用关系在 D06 实现。
- 标题和路径允许改变，记录 ID 保持稳定。用户界面显示中文名称，不要求直接编辑 ID。
- Markdown 正文保存镜头说明，属性保存结构化字段；布局只保存记录映射、位置、尺寸与视口。
- 使用宿主提供的属性操作能力与成熟解析工具，保留不归插件所有的属性和正文；损坏或未知版本记录明确提示，不能自动重建并覆盖。
- 约定笔记改名、移位或删除后如何刷新索引，保证读取同一记录时不会产生副本。

验收：中性夹具往返保存后内容不丢失；重命名仍能定位同一 ID；损坏记录不被写空。数据测试的通过仅证明对应逻辑，宿主事件需在 D03 实测。

## D03：双向读写与保存反馈

预期修改：镜头卡界面、资料库读写模块、自定义视图的订阅与卸载。

- 在画布视图编辑一张镜头卡的标题和内容，保存到对应笔记。
- 原生笔记或 Agent 修改文件后，视图更新；存在未保存编辑时比较变化，避免覆盖用户正在编辑的字段。
- 写入基于最新内容，只更新所属字段。异步处理和过期表单需要冲突检查，不能仅凭使用 Vault API 就宣称无并发问题。
- 保存状态至少区分“保存中、已保存、保存失败、内容冲突”；失败保留编辑内容并提供重试。
- 同一场次可以在两个标签页查看；正式数据共享，焦点和选中状态属于各自视图。

P0-A 验收证据：

1. 视图改内容 → 笔记更新；笔记改内容 → 视图更新。
2. 关闭重开 Obsidian，内容仍在。
3. 两个视图编辑同一记录时，不发生静默覆盖。
4. 模拟保存失败后界面保留输入并明确提示，重试能恢复。
5. 记录实际运行的检查、宿主版本和未验证项到 `docs/validation/p0-a.md`，该文件在验证发生时创建。

## D04：从原画布复用文件

先完成 D03，使显示组件可以接入确定的读写接口；复用代码到本目录后再处理宿主差异。

| 原画布文件 | 本仓库拟定目标 | 复用与适配范围 |
| --- | --- | --- |
| `web/src/components/canvas/infinite-canvas.tsx` | `src/canvas/infinite-canvas.tsx` | 背景、平移、缩放；调整事件焦点、所属窗口和光标范围 |
| `web/src/lib/canvas-theme.ts` | `src/canvas/theme.ts` | 保留画布视觉语言，接入 Obsidian 明暗主题，样式仅影响画布容器 |
| `web/src/types/canvas.ts` | `src/canvas/types.ts` | 只引入显示层所需的坐标、视口、节点和连线类型；正式镜头记录仍使用本仓库模型 |
| `web/src/components/canvas/canvas-node.tsx` | `src/canvas/shot-card.tsx` | 提取当前切片需要的卡片操作，不带入生成、Agent 或完整节点插件宿主 |
| `web/src/components/canvas/canvas-connections.tsx`、`web/src/lib/canvas/canvas-node-geometry.ts` | `src/canvas/` 下对应模块 | 连线切片实际需要时再引入，不为将来用途预先复制 |

每次引入时新增或更新 `docs/source-reuse.md`，记录源仓库 remote、提交号、源文件路径、实际源文件 SHA-256、目标路径、适配点及许可证。包含未提交变化的源文件必须注明工作区快照；实际引入代码时同时保留相应许可证文本。D01–D03 没有转移原画布源码，不能提前声称已复用。

不整体导入 `project.tsx`、`main.tsx`、全局 AppProviders、原浏览器持久化 store 或 RunningHub 服务。原文件继续保留；后续针对 Obsidian 的修改只落在本仓库目标文件。

P0-B 验收：卡片位置及视口重开恢复；输入焦点在旁边笔记时空格等快捷键正常；主题切换不改其他面板；视图关闭后监听被清理。

## D05：素材接入

预期新增：`src/storage/vault-media.ts`、按需拆分的素材预览组件。

- 首轮关联资料库内的参考图和短视频，保存稳定 ID 与资料库相对路径，临时播放地址不写入正式记录。
- 使用真实元数据，无法解析时显示未知；缺失文件或不支持的视频明确提示。
- D05 通过中文选择器关联库内素材，明确归属镜头；不自动采用、不覆盖源文件。拖拽导入及真实项目的默认复制／关联方式留待正式导入切片。
- 外置素材支持单独验证；库内视频可播放不能代替外置硬盘访问与重新定位验证。

P0-C 验收：一张镜头卡关联一张图、一段视频，比例与播放正确，重开仍可定位；改名和文件缺失得到明确处理。通过后记录 `docs/validation/p0-c.md`，并检查 P0 整体流程。

## 后续阶段与验证规则

D06–D08 的详细范围沿用 [总计划的 P1–P3](plan.md)。每个阶段开始前，基于已实现结构补充具体文件范围与验收记录；不在前阶段提前搭建接口或兼容层。

- 文档：检查链接、路径约定、进度标记和范围一致性。
- 实施切片：执行类型检查、构建及必要的保存／关联逻辑验证；现有命令以 `package.json` 为准，P0-A 的执行结果已记入验收记录。
- 宿主验收：在隔离测试库验证实际读写、重开、输入边界和媒体能力；构建通过不等于 Obsidian 内通过。
- 仅在新改动、失败或未解决问题出现时追加或重复测试。
- 完成一个切片后更新待办和验证证据；Git 提交与远端同步分别报告，不把同步当作功能完成。
