# 0.7.0 剧本项目库验收

日期：2026-09-12。范围见 [项目库调整](../project-library-plan.md)。

- `npm run typecheck`、`npm run build` 通过。
- `npm test`：59 项通过；6 项新增覆盖旧内容只读归属、项目资产直接创建、跨场次复用、跨项目拒绝、AI 和画布范围、项目默认 Skill 和工作区状态恢复。
- `npm run test:projects`：9 项隔离 Obsidian 操作通过，包括创建两个项目、简化剧本编辑、资产分类与参考图确认、跨场次共享、项目隔离、独立资产提取及 Skill 选择、分镜生成／编辑／取消、素材导入／删除和旧笔记逐字核对。

脚本只操作 `.local/test-vault`，使用中性剧本和本机模拟文字接口。结果及截图保存在忽略提交的 `.local/projects-*`。0.6.0 及更早的宿主脚本保留历史入口断言，不作为当前界面验收；数据逻辑继续由 `npm test` 回归。

- 完全重启隔离实例后 `npm run test:projects:reopen`：2 项通过，恢复项目和分镜工作区、人工编辑、资产用途及媒体，旧项目笔记仍逐字一致。
- 已查看资产库与分镜页截图：资产列表和编辑区并排，分镜使用独立宽区域，未发现水平溢出。

## 正式资料库

已更新既有资料库，原插件备份位于 `.obsidian/obcanvas-backups/2026-09-12T01-53-46-461Z-0.6.0/plugin/`，不在插件扫描目录内。安装仅替换 `main.js`、`styles.css`、`manifest.json` 和两份许可证。16 份项目文件在安装前后 SHA-256 一致。

完全退出并重开正式 Obsidian 后，用原生界面确认：显示 0.7.0，项目库列出“原有剧本项目”，包含 2 份剧本和 7 项拍摄资产；进入后五个工作区可见，资产库显示人物 1、场景 1、道具 5。没有为正式验收发起模型请求或修改剧本／资产内容。停留在原有项目的资产库供用户实操。

构建与安装 `main.js` SHA-256：`c24f79c02123366c41bcae9d6691f71696be600e81335ca7f7006ccc6504b46e`。本机详细安装核对记录保存在忽略提交的 `.local/project-library-install.json`。

## 范围限制

尚未验证真实文字模型的创作质量。未增加旧库自动拆项目、跨项目转移或合并、删除整个项目。原有素材位置保持不变。正式镜头入卡、视频生成和截图续接仍按 DS 计划验收。
