# R0：自由画布操作原型验证

日期：2026-09-11。范围为 `docs/prototypes/free-canvas/`；未修改或重新安装 0.2.0 插件，未读取或写入正式项目素材。用户对交互方向的最终试用确认尚未完成。

## 已执行

- `node --check docs/prototypes/free-canvas/app.js`：通过。
- Codex 浏览器实际操作：初始示例显示七张卡片和五条用途连线，详情栏默认收起。
- 点击镜头显示拍摄理由、景别与关联素材；修改标题后卡片立即更新。
- 从关键帧连接到另一镜头，待办由「缺少起始帧」变为「参考已连接」；这仅验证操作和关系，不表示参考画面适合该镜头。
- 点击待办定位镜头；移除新增关系后，待办恢复缺少起始帧，两张卡片均保留。
- 视频采用和重做演示能更新对应镜头状态。检查中修正了所有候选被退回时仍显示待选片的问题。
- 拖动剧本卡，屏幕位置由约 `(116, 249)` 变为 `(181, 299)`，内容和卡片数量不变。
- 空白画布提供「放入剧本」入口；粘贴中性剧本文字后卡片显示内容，无需先建立镜头。
- 检查深浅主题截图与桌面布局。窄侧栏首次总览曾缩到 20%，已改为低于 650px 时以 90% 显示首卡，可平移到其他对象；工具按钮换行。
- 检查后恢复浏览器视口覆盖，实际页面宽度 319px 时显示 90%；运行日志未见 JavaScript 错误。

## 实际边界

- 预览不保存数据；刷新、切换空白/示例都会恢复演示状态。页面全程标明限制。
- 图片/视频文件入口已写入原型，但本轮未用实际文件验证选择、拖入和播放，不列为通过项。没有接入 Obsidian 素材库。
- 剧本示例和「为什么这样拍」是静态演示文案，未调用 Skill 或 AI。平面图与构图是示意图，视频是占位卡。
- 连线与候选演示仅供理解操作，正式数据约束、共享视频在各镜头独立采用、草稿恢复和并发写入需在 R1/R2 使用既有保存层验证。
- 不包含截图、自动拆镜、生成调用和实际制作包导出。没有用此原型替换正式插件。

本次没有更改 `src/` 或构建配置，因此未重复运行插件宿主验收或打新安装包。R1 接入 Vault 后须重新验证数据保留与宿主交互。
