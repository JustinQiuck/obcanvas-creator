# 项目工作约定

## 目录与范围

- 当前本地开发根目录为 `/Users/aliceboy/项目文件/闲来无事/obcanvas-creator`。源码、计划、验证记录及从原画布复用的文件都以本仓库为准。
- `/Users/aliceboy/项目文件/画布` 是复用来源。按需复制文件到本仓库再适配，保留原文件和未提交修改；不要把构建、运行或开发依赖指向仓库外的画布目录。
- 引入源文件时记录来源路径、来源提交、实际内容校验值、目标路径和适配说明；工作区有未提交改动时不能只记录 HEAD。保留适用的许可证和署名。
- 当前路线和范围见 `docs/plan.md`，技术设计见 `docs/obsidian-integration.md`，实施顺序见 `docs/development-plan.md`，进度以 `docs/TODO.md` 为准。

## 实施约定

- 以用户当前指令为准，只完成必要改动。实质编辑前说明目标、范围和验收标准；只在用户明确要求时使用子代理。
- 每次推进一个可验收切片，保留无关改动，不顺手重构原画布或导入整套网页业务。
- 用户界面、引导、状态和错误提示使用中文，不要求用户手写底层属性或配置。
- Obsidian 资料库文件保存正式记录；画布布局单独保存，不能维护第二份镜头内容或采用决定。
- RunningHub 保持外部生成，不引入工作流提交、轮询、API Key 管理或收费调用。
- 先使用隔离测试资料库和中性测试素材。真实资料库、私人素材、凭据和本机运行数据不进入 Git。

## 验证与交付

- 按改动执行必要验证。文档修改检查链接和一致性；功能修改执行相关静态检查、构建及必要的逻辑验证，并单独记录 Obsidian 内的实际验收。
- 代码已写、构建通过、实机通过是不同状态。未验证项不能勾选完成，失败项需保留证据及复现步骤。
- 验证发现本次改动引入的问题时修复并针对性复验，不无故重复或扩大检查。
- 提交只暂存明确属于本任务的文件。远端同步按用户授权执行，不把本地开发授权扩展为发布、部署或改动其他项目。
- 简明报告结果、验证和剩余限制，不以未来功能代替本次交付。
